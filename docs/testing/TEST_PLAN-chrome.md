# תוכנית בדיקה — תוסף "לץ המשפט"

צ'ק-ליסט להרצה **בכל עדכון** לפני העלאה לחנות. מכסה את כל הפיצ'רים + מצבי הקיצון
שכבר ראינו נשברים. סמן/י ✅ / ❌ ליד כל סעיף.

> **טיפ rate-limit:** נט המשפט חוסם פרצים של בקשות. אם עשית הרבה הורדות/בדיקות
> ברצף, **חכה 15–30 דקות** לפני בדיקת הורדה גדולה, אחרת הסשן חסום וכל בדיקה תיכשל
> בלי קשר לקוד. אל תריץ שתי הורדות במקביל.

---

## 0. טעינה ותקינות בסיסית
- [ ] `chrome://extensions` → Reload → **מספר הגרסה תואם** ל-`manifest.json`.
- [ ] אין שגיאות אדומות תחת "Errors" בכרטיס התוסף.
- [ ] פתח DevTools Console בעמוד נט המשפט → **אין שגיאות אדומות** בטעינה.
  - ⚠️ *רגרסיה שנשברה:* TDZ — `let` שניגשים אליו לפני שורת ההגדרה → **שום דבר לא
    מופיע בכלל**. `node --check` לא תופס את זה; רק טעינה אמיתית + קונסול.

## 1. טוגל ראשי ("לץ המשפט")
- [ ] הטוגל מופיע בכותרת (ליד "יציאה" בפורטל המאומת).
- [ ] לחיצה **מדליקה/מכבה** — מצב פועל מציג כובע ליצן על הלוגו + כל הווידג'טים;
      מצב כבוי משאיר **רק את הטוגל**, מסתיר כובע + פאנלים.
- [ ] הטוגל **לא קופץ מקום** בין הדלקה לכיבוי.
- [ ] כפתור הסנכרון (אם בעמוד שמתאים) **עדיין מגיב** ללחיצה כשהטוגל לידו.
  - ⚠️ *נשבר בעבר:* הטוגל "בלע" קליקים של כפתור הסנכרון; קפיצת מיקום; לא הופיע
    בעמודים בלי user-strip (קפץ למטה-ימין).
- [ ] בפורטל הציבורי (`www.court.gov.il/ngcs.web.site/...`) — הטוגל ממוקם תקין.

## 2. בחירת מסמכים (כל סוגי המסכים)
לכל סוג: פתח את הרשימה, ודא שהפאנל הכחול מופיע ושיש checkbox בכל שורה.
- [ ] **החלטות** (DecisionList)
- [ ] **פסקי דין**
- [ ] **פרוטוקולים** (ProtocolFileViewer) — כולל **תמלילים** (עמודת תמליל)
- [ ] **בקשות / כתבי טענות** (נתיב per-doc)
- [ ] **תיק נייר**
- [ ] **מוצגים**
- [ ] **"סימון הכל" סופר את כל המסמכים בכל העמודים**, לא רק את העמוד הנוכחי.
  - ⚠️ *הבאג הגדול:* בפרוטוקולים "סימון הכל" סימן רק 15 מתוך 165 (עמוד אחד).
    בדוק במיוחד תיק עם **>1 עמוד דפדוף** (165 פרוטוקולים, 18 עמודים).
- [ ] בפרוטוקולים: המספר תואם למסמכים שיש להם checkbox באתר (מסמכים שלא פורסמו /
      בלי checkbox **לא** נספרים).

## 3. הורדה — פורמטים (הליבה)
> בתיק קטן (~5 מסמכים) קודם, ואז תיק גדול (>100) לבדיקת rate-limit.

### 3a. Word רגיל (אצווה)
- [ ] סימון הכל → פורמט **Word** → הורדה → **כל המסמכים יורדים** (אצוות של 5).
  - הערה: Word מאחד ~5 מסמכים ל-docx אחד → התוצאה היא ~N/5 קבצים, לא קובץ למסמך.
- [ ] עובד גם ל-**>100 מסמכים** (33 בקשות בלבד — מתחת לחסימה).

### 3b. PDF (קובץ למסמך + אינדקס)
- [ ] תיק קטן: סימון הכל → **PDF** → הורדה → ZIP עם PDF לכל מסמך + `index.csv`.
- [ ] **תיק גדול (>100):** ההורדה רצה איטי (1.5ש׳/מסמך), ואם מגיעה לחסימה — מציגה
      **"סבב ניקוי: ממתין Xש׳…"** (120→180→240) ואז ממשיכה את הנכשלים.
  - ⚠️ *נשבר בעבר:* בלי האטה — נתקע ב-~28–38; עם האטה בלבד (בלי סבבי-ניקוי) —
    עצר ב-~107. **בדוק שמגיע לכל 161** (אולי בכמה סבבים עם המתנות).
- [ ] `index.csv` נמצא בכל חלק ZIP, מכיל גם את הנכשלים (מסומנים "נכשל").

### 3c. Word עם אינדקס (איטי, קובץ למסמך)
- [ ] ✔ "עם אינדקס" → Word → הורדה → **קובץ docx לכל מסמך** עם שם אינפורמטיבי +
      `index.csv`.
- [ ] תיק גדול: אותם סבבי-ניקוי כמו PDF, מגיע לכל 161.
  - ⚠️ *נשבר בעבר:* מצב איטי החזיר 0 (rate-limit מצטבר); כעת עם סבבי-ניקוי.

### 3d. מסמכים חסומים ל-Word
- [ ] תיק עם מסמך שלא ניתן ל-Word ("אינם ניתנים להורדה") → האצווה **מדלגת עליו
      ולא תוקעת את השאר** (נופל-לאחור פר-מסמך).

## 4. Resume (המשך מריצה קודמת)
- [ ] הורד תיק גדול שחלק נכשל (סטטוס "⚠ הסתיים — X · Y נכשלו").
- [ ] לחץ **"הורדה" שוב** → **מדלג על מה שכבר ירד וממשיך רק את הנכשלים**.
  - ⚠️ *נשבר בעבר:* בסיום הקוד מחק את מעקב ההתקדמות → לחיצה חוזרת הורידה הכל מחדש.
- [ ] אם **כל** המסמכים ירדו (0 נכשלו) → לחיצה חוזרת אומרת "כבר ירדו".
- [ ] **פג תוקף סשן באמצע:** הסטטוס אומר "פג תוקף הסשן… התחבר ולחץ הורדה שוב",
      **לא** "נכשל". אחרי התחברות מחדש — ממשיך מהנקודה.

## 5. ZIP מקומי
- [ ] תיק גדול מתפצל ל-**חלקים** (חלק1, חלק2…) ב-~150MB / 80 מסמכים.
- [ ] אם Chrome מציג **"Download multiple files"** → צריך ללחוץ **Allow** (אחרת
      חלקים נחסמים).

## 6. Google Drive
- [ ] **חיבור:** "התחבר לחשבון Google" → OAuth → "✓ מחובר".
- [ ] **בורר תיקיות:** "בחר / שנה" → לחיצה על **שם** תיקייה נכנסת אליה; כפתור
      **"✓ בחר"** בוחר אותה; שם "תיקיית יעד" מתעדכן.
- [ ] **העלאה לתיקייה שנבחרה:** הורד עם Drive מסומן → הקבצים נוחתים **ישירות
      בתיקייה שבחרת** — **בלי תת-תיקייה אוטומטית**. הסטטוס מציג את שם התיקייה.
  - ⚠️ *נשבר בעבר:* התוסף יצר תת-תיקייה "<תיק> - <רשימה>" (לעיתים ב-root) ללא קשר
    לבחירה; הסטטוס הציג את שם התת-תיקייה (מטעה).
- [ ] **מניעת כפילויות:** ✔ "העלה רק חדשים" → הורדה חוזרת **מדלגת** על קיימים.
- [ ] index.csv עולה ל-Drive גם.

## 7. דיונים / יומן שופט
- [ ] עמוד דיונים: פאנל מופיע, בחירה + הורדה עובדים.
- [ ] **יומן שופט — תצוגה חודשית:** כותרות הימים (א׳–ש׳) **מיושרות בדיוק** עם
      העמודות; כל 7 העמודות נראות.
  - ⚠️ *נשבר בעבר:* טקסט דיון ארוך דחף עמודה והגריד גלש → עמודות ימין נחתכו
    (תוקן עם `minmax(0,1fr)`). בדוק עם דיונים בעלי טקסט ארוך.
- [ ] שורה 1 = טווח תאריכים; שורה 2 = כל הפעולות (תצוגה / הורדה / הגדרות).

## 8. פופאפ + הגדרות
- [ ] פופאפ נפתח, שם "לץ המשפט", טאבים מסמכים/דיונים/יומן Google.
- [ ] בחירת יעדים (ZIP / Drive) נשמרת ומשתקפת בפאנל בדף.
- [ ] טוגל פורמט (Word/PDF) + "עם אינדקס" נשמרים.
- [ ] **אין** תיבת "שרת/API" גלויה (מוסתרת).
- [ ] קישורי פרטיות/תנאים → z-g.co.il.

## 9. שני הפורטלים
- [ ] **מאומת:** `securesso.court.gov.il/Ngcs.Web.Secured/*` — הכל עובד.
- [ ] **ציבורי:** `www.court.gov.il/ngcs.web.site/*` — התוסף נטען (לבדיקת reviewers).

## 10. טקסטים (דו-מגדרי)
- [ ] כל הכפתורים/הסטטוסים בשפה דו-מגדרית (סימון/ניקוי/הורדה/"ניתן לסמן…").

## 11. אריזה ל-CWS (`node build-zip.js`)
- [ ] גרסה ב-`manifest.json` **עלתה** מהגרסה שבחנות.
- [ ] ה-ZIP נבנה ל-`dist/extension-v<גרסה>.zip`.
- [ ] **שמות עם לוכסן `/`** (לא `\`) — אחרת CWS/כרום לא מוצאים קבצים.
  - ⚠️ *נשבר בעבר:* Compress-Archive ייצר `\`; תוקן עם JSZip.
- [ ] **אין קבצים זרים:** ללא `*.md`, `*.zip`, `marketing/`, `*.map`, source.
  - ⚠️ *נשבר בעבר:* הורדת פרוטוקול נשמרה בטעות ב-`icons/` ודלפה ל-ZIP (467KB).
    בדוק שתיקיית `icons/` מכילה **רק** 16/48/128.png.
- [ ] `manifest.json` — **אין** `optional_host_permissions` רחב (`https://*/*`).
- [ ] טען unpacked מ-ZIP מפורס → נטען בלי שגיאות.

---

## בדיקת עשן מהירה (5 דק׳, למינור-עדכון)
1. Reload → גרסה תואמת, אין שגיאות קונסול.
2. טוגל הדלקה/כיבוי.
3. תיק קטן (~5): סימון הכל → PDF → הורדה → ZIP + index תקינים.
4. אותו תיק → Word → הורדה תקינה.
5. `node build-zip.js` → ZIP נקי עם לוכסנים תקינים.

## בדיקה מלאה (לפני עדכון משמעותי)
כל הסעיפים 0–11, כולל **תיק גדול (>150 מסמכים)** לשני המצבים (PDF + Word עם
אינדקס) עד הסוף, ובדיקת Drive מלאה.
