# היסטוריית גרסאות — לץ המשפט

כל הגרסאות של התוסף, בשני הדפדפנים, במקום אחד.
התאריכים הם תאריכי הפיתוח (commit); תאריך הפרסום בחנות עשוי להיות מאוחר יותר.

## מצב נוכחי

| דפדפן | גרסה | סטטוס |
|---|---|---|
| **Chrome / Edge** | **0.19.3** | בפיתוח — לא הוגש לחנות |
| **Firefox** | **0.19.3** | בפיתוח — לא הוגש ל-AMO |

**שני הדפדפנים נבנים מעץ מקור אחד (`src/`) ותמיד באותה גרסה.** אין יותר פער
פיצ'רים ביניהם: `npm run build:all` מפיק את שתי החבילות מאותו קוד.

---

## Chrome

### 0.19.3 — 2026-08-07 · דיווח אמין + סנכרון יומן שמעדכן
לא הוגש לחנויות. מספר הגרסה קיים כדי לאמת איזה בילד טעון (`document.documentElement.dataset.cdVersion`).

#### סנכרון היומן מעדכן במקום לשכפל
- **דיון שנדחה יצר אירוע שני ביומן במקום להזיז את הקיים.** ה-`iCalUID` נבנה
  מגיבוב של `caseId+תאריך+שעה+סוג דיון` — כלומר מה**תוכן** של האירוע ולא
  מהזהות שלו. `events.import` מזהה אירוע קיים לפי ה-UID, אז ברגע שהתאריך או
  השעה השתנו נוצר UID חדש, נוצר אירוע חדש, והישן נשאר. שום קריאה לא מחקה או
  עדכנה אותו אי פעם.
- **מסלול שכפול שני:** ה-UID כלל גם את סוג הדיון, ולא כל מסכי הדיונים מציגים
  עמודת "סוג דיון" — כך שסנכרון מהעמוד האחד ואז מהשני שכפל הכול בלי שום
  שינוי אמיתי.
- הסנכרון עובר להתאמה אמיתית: כל אירוע שנכתב מתויג ב-`extendedProperties`,
  והסנכרון מושך את האירועים הקיימים של אותו תיק, מתאים אותם לדיונים הנכנסים
  (לפי תאריך+שעה, ואז לפי תאריך, ואז לפי סדר כרונולוגי — מה שהופך דחייה
  לעדכון), ואז מעדכן / מוסיף / מסמן. ההתאמה מוגבלת לתיקים שבסנכרון הנוכחי
  ולטווח התאריכים שלהם, כדי שסריקה חלקית לא תיגע במה שלא נסרק.
- **כפילויות קיימות מתמזגות אוטומטית.** אירועים שנוצרו לפני התיקון אינם
  מתויגים, אבל כל UID שהתוסף ייצר אי פעם מסתיים ב-`@court-downloader` — לפי זה
  הם מזוהים ונבלעים בסנכרון הבא.
- דיון שנעלם מנט המשפט מסומן `בוטל: …` והזמן משוחרר, אבל האירוע **נשאר**.
  לא נעשה שימוש ב-`status=cancelled`, שכן באירוע לא-חוזר גוגל מתייחס לזה
  כמחיקה והאירוע נעלם — ההפך ממה שהתבקש.
- אירוע שאינו של התוסף לא נגלע לעולם.
- ההרשאות לא השתנו (`auth/calendar` כבר כלל קריאה) — אבל הניסוח בניירת של שתי
  החנויות עודכן, כי הסנכרון עכשיו גם **קורא** אירועים.
- 451 בדיקות jsdom.

#### דיווח אמין על מה שנט המשפט לא מאפשרת
תיקוני אבחון בעקבות בדיקה חיה מול שני הפורטלים על אותו תיק.

- **PDF בפורטל הציבורי — סירוב של הצופה כבר לא מדווח כחסימת קצב.** כשהפורטל
  מפסיק לפתוח מסמכים, ה-postback חוזר 200 עם רשימת ההחלטות מרונדרת מחדש ובלי
  `DocumentNumber`. התוסף פירש את זה כחסימת קצב של נט המשפט ונכנס לעד 8 סבבי
  המתנה מתארכים — כ-20 דקות שבהן הוא מכריז על חסימה שלא קרתה. עכשיו הוא מזהה
  שכל הכשלים מהסוג הזה, עוצר אחרי מעבר אחד, ומדווח מה קרה באמת. רשומת ההתקדמות
  נשמרת, כך שלחיצה חוזרת על "הורדה" ממשיכה מהנקודה.
  **הבהרה:** בבדיקה חיה על 0.19.3 הורדת PDF בפורטל הציבורי **עבדה** — הזרימה
  כן נתמכת שם. הסירוב מופיע כשההגנה של האתר מפני גישה אוטומטית נכנסת לפעולה,
  ואז הוא זמני. ניסוח מוקדם יותר של הפריט הזה טען שהתכונה אינה נתמכת בפורטל
  הציבורי — זו הייתה מסקנה שגויה מ-A/B שרץ על סשן שכבר היה מוגבל.
- **הורדת Word — "0 קבצים" לא הייתה תקלה, אלא דיווח מטעה.** נט המשפט מציעה כ-Word
  רק מיעוט מההחלטות (בתיק שנבדק: 2 מתוך 19), ואין שום שדה בנתוני העמוד שמנבא
  אילו. בקשת ה-Word של התוסף תקינה — אומת מול מסמך שהורד ידנית בהצלחה.
- **הוסר סיווג ה"חסום" השגוי.** הקוד קבע "חסום ע"י נט המשפט" לפי נוכחות
  `btnDownloadWordDocs2` — קישור מוסתר שהמסך מרנדר **בכל** תשובה, כולל תשובות
  מוצלחות. לכן כל כשל — סשן, רשת, שגיאת שרת — נצבע כחסימה של בית המשפט, בלי
  ניסיון חוזר ובלי שהמשתמש יוכל להבחין. עכשיו מדווח "אינם זמינים כ-Word",
  והפניה למצב PDF.
- **`keepAliveUrl()` פגע בעמוד האפליקציה במקום בנכס סטטי.** הסלקטור דרש את
  המחרוזת `court.gov.il` בתוך התכונה, אבל שני הפורטלים מפנים לנכסים בנתיב יחסי
  — ולכן הוא לא התאים לאף אלמנט באף פורטל ונפל חזרה ל-`location.href`. ה"פינג
  הקל" היה בפועל GET לעמוד ASP.NET עם פרמטר `?_cdka=` מזויף, מיד עם לחיצת
  "הורדה" וכל 3 דקות. עכשיו נבחר קובץ סטטי אמיתי, ואם אין כזה — אין פינג.
- **`header-sync.js` עדיין קידד קשיח `/Ngcs.Web.Secured/PersonalAreaPage.aspx`**
  בכל דומיין. בפורטל הציבורי זו כתובת שה-WAF עונה עליה
  "זוהתה פעילות בלתי מורשת" (אומת חי). בדיקת הרגרסיה שחסמה נתיבים מאובטחים
  קשיחים פטרה את המסך הזה במפורש — וזו בדיוק הפרצה ששימרה את הבאג; הפטור הוסר
  והבדיקה הודקה.
- שגיאות פר-מסמך כבר לא נבלעות: הסיבה שנצפתה בפועל מגיעה גם לסיכום וגם ל-CSV.
- נמחק `startDocDownload()` — קוד מת בן ~55 שורות ששכפל את מסלול ה-Word.
- 424 בדיקות jsdom.

### 0.19.2 — 2026-08-01 · הורדת מסמכים בפורטל הציבורי
- **הורדת מסמכים בפורטל הציבורי (`www.court.gov.il`) מעולם לא עבדה — בשני
  הדפדפנים.** נקודת הקצה של הצופה נבנתה תמיד עם שורש היישום המאובטח, ולכן
  בדומיין הציבורי הבקשה הלכה לנתיב שהמארח הזה לא משרת. ה-WAF של האתר החזיר
  "חסימת בקשה לא מורשית", הקוד קרא את זה כחסימת קצב של נט המשפט, ונכנס
  ל"סבב ניקוי" של דקות בלי שהורד ולו מסמך אחד. אומת חי מול השרת: הנתיב הציבורי
  מחזיר `application/json`, המאובטח מחזיר את דף החסימה.
- נוסף `CD.appRoot()` (`shared/constants.js`) שגוזר את שורש היישום מהעמוד עצמו,
  והצופה משתמש בו. תשובת 200 שאינה JSON מדווחת ככזו במקום להיראות ככשל רגיל.
- אותו נתיב מאובטח קשיח היה גם ב-GET הישיר של דוח יומן השופט — תוקן. שם הוא היה
  מוסתר בזכות נפילה-לאחור לתפריט, אבל כל ריצה בזבזה בקשה חסומה.
- בדיקה חדשה נכשלת אם מקשיחים שוב נתיב מאובטח בבקשה. 414 בדיקות jsdom.

### 0.19.1 — 2026-08-01 · תיקוני הורדה קריטיים לפיירפוקס
- **הורדת מסמכים בפיירפוקס נכשלה** בשגיאה "ספריות הקובץ לא נטענו". הסיבה: בתוסף
  של כרום `window`, `globalThis` ו-`this` בסקריפט תוכן הם אותו אובייקט, ובפיירפוקס
  לא — `globalThis` הוא ה-sandbox של התוסף ו-`window` הוא עטיפת Xray של הדף. שתי
  הספריות המצורפות בוחרות אחרת: `jszip.min.js` נתלית על `window`, ואילו
  `jspdf.umd.min.js` נתלית על `this` העליון, כלומר על ה-sandbox — ולכן
  `window.jspdf` נשאר undefined בפיירפוקס ומסלול ה-PDF/ZIP נעצר. הבאג היה קיים
  מאז איחוד עצי הקוד ולא נבע מ-0.19.0.
- הפתרון: `CD.vendorGlobal()` ב-`shared/browser-compat.js` — התפר הקיים לשוני בין
  הדפדפנים — מחפש בשני המקומות, וכל צרכני הספריות עברו דרכו. בכרום ההתנהגות לא
  משתנה. בדיקה חדשה חוסמת חזרה של `w.JSZip` / `w.jspdf` ישירות בקוד.
- **"הורד CSV" בלשונית "תיקי מקור" לא הוריד כלום בפיירפוקס** — עוגן ההורדה נלחץ
  לפני שצורף ל-DOM, ופיירפוקס מתעלם מלחיצה על אלמנט מנותק. תוקן לפי אותו דפוס
  `saveBlob()` שכל שאר ההורדות בתוסף כבר משתמשות בו, ובדיקה חדשה חוסמת חזרה.
- "העתק" נופל חזרה ל-`execCommand('copy')` אם ה-Clipboard API חסום.
- מסך ההמתנה שורד מצב שבו פיירפוקס מריץ את `document_start` לפני שנוצר
  `<html>` — קודם הוא פשוט לא הופיע שם.
- 409 בדיקות jsdom.

### 0.19.0 — 2026-08-01 · **חיפוש לפי תיק מקור**
- **איתור תיק מהיר — חיפוש לפי תיק מקור**: לצד שדה מספר התיק נוספה רשימה סגורה
  של סוגי תיק המקור של האתר (דו"ח תעבורה, תיק משטרתי (פל"א), תיק הוצל"פ, הודעת
  קנס, עניינים מקומיים, דו"ח עירייה, עמ"ק ישן, כונס הנכסים הרשמי, בית דין מנהלי
  לתעבורה). ברירת המחדל — הדבקת מספר תיק — לא השתנתה.
- **לשונית חדשה בחלונית התוסף: "תיקי מקור"** — מדביקים רשימת מספרי תיק מקור
  ומקבלים טבלה של תיקי בית המשפט שנמצאו, שורה לכל תיק (אותו מספר מקור חוזר על
  עצמו כשנמצאו כמה תיקים). כולל העתקה ל-Excel, ייצוא CSV, ועצירה באמצע. העבודה
  והתוצאות נשמרות ב-`chrome.storage`, כך שאפשר לסגור את החלונית תוך כדי הרצה.
- שני הפיצ'רים מנגנים את **הרצף של האתר עצמו**: שדות מסך "תיקים לפי מס' תיק מקור"
  הם AutoPostBack, והשרת בונה את הקריטריון מסבבי ה-postback שלהם. מילוי שני
  השדות ולחיצה אחת על "אישור" מחזיר 0 תוצאות גם לתיק שקיים — לכן כל שלב נשלח
  בנפרד. שחזור הרצף ב-fetch ברקע נבדק ונפסל (השרת אינו משחזר את מצב החיפוש).
- לחיצה על פקדי `javascript:` של האתר מתוך התוסף אינה עובדת (ה-URL רץ בעולם
  המבודד): במקומה נשלח postback אמיתי דרך `__EVENTTARGET` + `form.submit()`.
- מסך ההמתנה (`content/ext-busy.js`, רץ ב-`document_start`) מסתיר את טעינות
  הביניים כדי שהמסך לא ירצד, ויורד אוטומטית בכל מסלול שגיאה.
- לאחר חיפוש תיק מקור הפאנל מדווח כמה תיקים נמצאו — מסך תוצאות ריק כבר לא נראה
  כמו מסך שרק מולא.
- 404 בדיקות jsdom (מ-302).

### 0.18.37 — 2026-07-10
- אייקון חדש: כובע הלץ יושר ומורכז (במקום מוטה 7°), בכל הגדלים.
- סדרת צילומי מסך ממותגת חדשה לחנות (1280×800): שער, מסמכים, יומן שופט, ייצוא דיונים, סנכרון יומן, איתור תיק ומועדפים.

### 0.18.36 — 2026-07-08
גרסת התיקונים הגדולה, אחרי אימות מקצה-לקצה בשני פורטלי נט המשפט:
- **יומן שופט/ת — מנוע איסוף אחד**: כל מסלולי היומן מפעילים את ה-runner בטאב ייעודי (postbacks מעמודים חיים). מנוע ה-fetch ברקע הוצא משימוש — הוא גרם לימים משוכפלים/ריקים.
- אימות יום-מרונדר, קצב אנטי-WAF, ניסיון חוזר על קידוד פגום, הטבעת תאריכים, דה-דופליקציה, וניסיון חוזר לקסקדת השופטים.
- היומן שורד רענון דחוי של האתר (התוצאה נשמרת ונפתחת מחדש אוטומטית).
- תמיכה מלאה גם בפורטל הציבורי (`www.court.gov.il`).
- שם השופט/ת נלקח מ-grid הדיונים (שם מלא) במקום מקיצור הבאנר.
- **מועדפים**: תוקן באג שבו הוספת תיק שני מחקה את הראשון; שם תצוגה מלא (סוג תיק + צדדים); רשימה מתקפלת.
- **איתור תיק**: תוקנה שגיאת "חובה להזין ערך בשדה סדרה"; תמיכה בשני סוגי ה-locator.
- שורת כלים מתחת לכותרת (ללא חפיפה), פריסה צד-לצד במסך רחב.
- חותמות גרסה ב-DOM לאבחון, `TESTING.md`, 271 בדיקות jsdom.

### 0.18.19–0.18.20 — 2026-07-08
- **תיקים מועדפים** (⭐) — שמירה מקומית, כניסה מהירה.
- רשימת בתי משפט סגורה ליומן השופט + seed סטטי של 90 בתי המשפט.
- שינוי מינוח: "פתיחת תיק" → "איתור תיק".

### 0.18.18 — 2026-07-06
- תיקון דחיית Chrome Web Store: הוסרה הפניה לקוד מרוחק והרשאת `downloads` שלא בשימוש.

### 0.18.11–0.18.17 — 2026-07-05/06
- יומן השופט עובר לאיסוף בטאב חדש (עוקף את חסימת ה-F5 של שער האבטחה).
- זיהוי בית משפט רק בוודאות; תמיכה בכל דומייני בתי המשפט וכל שיטות ההזדהות.
- התאמת שמות בתי משפט סובלנית לתחיליות ב'/ה'.
- הפופאובר לא נסגר בבחירת תאריך.

### 0.18.5–0.18.10 — 2026-07-04/05
- **איתור תיק מהיר** — הדבקת מספר תיק בכל פורמט.
- השלמת עקרון שתי התצורות: כל פיצ'ר עובד גם מוטמע וגם בחלונית צפה.

### 0.18.2–0.18.4 — 2026-07-03
- לשונית "מתקדם" בפופאפ (הגדרות מראה).
- הודעה ידידותית על "התוסף עודכן — רענן".

### 0.17.x — 2026-06
- עיצוב מחדש למערכת העיצוב של משפחת "לץ".
- הגדרות מראה: כובע ליצן, מיקום מוטמע/צף.
- שמות קבצים לפי סוג המסמך.
- OAuth היברידי ל-Drive (תיקיית ברירת מחדל + בורר מתקדם).
- תיקוני UI רבים בסרגל ההורדה ובמיפוי בחירת המסמכים.

---

## Firefox

### 0.19.3 — 2026-08-07 · דיווח אמין + סנכרון יומן שמעדכן
זהה לכרום — אותו עץ מקור, אותה גרסה. ראו את [פירוט 0.19.3 בכרום](#0193--2026-08-07--דיווח-אמין--סנכרון-יומן-שמעדכן).

### 0.19.2 — 2026-08-01 · הורדת מסמכים בפורטל הציבורי
זהה לכרום — אותו עץ מקור, אותה גרסה. ראו את
[פירוט 0.19.2 בכרום](#0192--2026-08-01--הורדת-מסמכים-בפורטל-הציבורי).
הבאג לא היה ספציפי לפיירפוקס; הוא צף שם כי שם נעשתה הבדיקה החיה.
אין שינוי הרשאות ואין שינוי מניפסט מול 0.19.1.

### 0.19.1 — 2026-08-01 · תיקון קריטי (**פורסם ב-AMO**)
- **הורדת מסמכים (PDF/ZIP) לא עבדה כלל בפיירפוקס** — "ספריות הקובץ לא נטענו".
  נחשף בבדיקה חיה לפני ההגשה; ראו
  [פירוט 0.19.1 בכרום](#0191--2026-08-01--תיקוני-הורדה-קריטיים-לפיירפוקס).
- 0.19.0 לא הוגשה ל-AMO מעולם.

### 0.19.0 — 2026-08-01 · **חיפוש לפי תיק מקור**
זהה לכרום — אותו עץ מקור, אותה גרסה. ראו את [פירוט 0.19.0 בכרום](#0190--2026-08-01--חיפוש-לפי-תיק-מקור).
- אין שינוי הרשאות. השינוי היחיד במניפסט הוא רשומת `content_scripts` שנייה
  ב-`document_start` (מסך ההמתנה), שעוברת להמרת הפיירפוקס כמו כל השאר.
- `web-ext lint`: 0 שגיאות.

### 0.18.37 — 2026-07-28 · **איחוד עצי הקוד**
פיירפוקס קפץ מ-0.17.15 ל-0.18.37 ומקבל את מלוא הפיצ'רים של הכרום — יומן שופט/ת,
איתור תיק מהיר, תיקים מועדפים, החלונית הצפה, וכל תיקוני הבאגים של 0.18.x:
- עץ הפיירפוקס הנפרד נמחק; שני הדפדפנים נבנים מ-`src/` באמצעות
  `build-zip.js --target=firefox`, שממיר את המניפסט בלבד (event page,
  `browser_specific_settings.gecko`, הסרת `oauth2`).
- שים תאימות `chrome`↔`browser` (`shared/browser-compat.js`) נטען ראשון בכל הקשר.
- ה-OAuth אוחד למימוש אחד שבוחר בזמן ריצה לפי יכולת הדפדפן: `getAuthToken`
  בכרום, `launchWebAuthFlow` בפיירפוקס (עם מטמון טוקנים ב-`storage.session`).
- חוזה הבנייה הדו-יעדית מקובע ב-`tests/dual-build.test.js` (סה"כ 289 בדיקות).
- `web-ext lint`: 0 שגיאות.

### 0.17.15 — 2026-07-27 (העץ הנפרד, לפני האיחוד)
- הצהרת **`data_collection_permissions: {required:["none"]}`** — דרישת חובה של מוזילה
  לכל הגשה חדשה מאז 2025-11-03.
- `strict_min_version` הועלה ל-140; `web-ext lint`: 0 שגיאות.
- נוסף [מדריך העלאה מלא בעברית](docs/firefox-amo/AMO_PUBLISH_GUIDE.md).

### 0.17.15 — 2026-06-09 (הפורט הראשוני)
- פורט מגרסת הכרום 0.17.15 לעץ נפרד: background הומר ל-event page, מזהה קבוע
  `court-downloader@z-g.co.il`, ו-OAuth דרך `identity.launchWebAuthFlow` —
  ראה [SETUP_FIREFOX.md](docs/firefox-amo/SETUP_FIREFOX.md).


## מדיניות שחרור

**כל גרסה חדשה מיוצרת לשני הדפדפנים במקביל.** אין יותר "גרסת כרום" ו"גרסת
פיירפוקס" נפרדות:

1. מפתחים ב-`src/` בלבד.
2. `npm test` — חייב להיות ירוק.
3. `npm run build:all` — מפיק את שתי החבילות מאותה גרסה.
4. בדיקה חיה בשני הדפדפנים, בשני הדומיינים, בשתי תצורות התצוגה
   ([צ'קליסט](docs/testing/TESTING.md)).
5. מעדכנים את ה-CHANGELOG הזה ומעלים לשתי החנויות.
